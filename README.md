# RailSpectra Capture releases

Public download repository for **RailSpectra Capture** (Windows desktop client).

- `TswCapture-win-x64.zip` — single-file self-contained `TswCapture.exe` for Windows x64

## Latest download

Stable build (redirects to newest release):

```text
https://github.com/abrowne93/railspectra-capture-releases/releases/latest/download/TswCapture-win-x64.zip
```

Configure the same URL in the platform release manifest (`capture_download_url`) or `TSW_CAPTURE_DOWNLOAD_URL`.
